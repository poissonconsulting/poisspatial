# Remove sfc columns

Remove specified sfc (geometry) column(s).

## Usage

``` r
ps_remove_sfcs(x, sfc_names = ps_sfc_names(x))
```

## Arguments

- x:

  An sf object.

- sfc_names:

  A character vector indicating the name of the sfc column(s) to remove.
