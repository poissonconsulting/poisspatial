# Test if numeric vector is long/lat

Tests if longitude is between 180 and -180. Object assumed to be in
format c(long, lat, long, lat) as in bbox.

## Usage

``` r
is_longlat_real(x)
```

## Arguments

- x:

  object to test.

## Value

flag.
